# Root config

- `next.config.ts` – Next.js config (loaded via `--config config/next.config.ts`).
- PostCSS and Tailwind configs stay at project root (`postcss.config.ts`, `tailwind.config.ts`) because the tooling expects them there.
